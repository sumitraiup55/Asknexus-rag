# Backend

This backend folder contains the server application for the AskNexus RAG repository.

- `src/` contains source code
- `.env` contains local environment variables
- `package.json` lists backend dependencies and scripts
